# MinIO Configuration
# Add these to your .env file

# MinIO credentials (change these for production)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# MinIO hostname for Caddy
MINIO_HOSTNAME=:8007
